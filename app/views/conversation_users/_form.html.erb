<%= form_with(model: conversation_user) do |form| %>
  <% if conversation_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation_user.errors.count, "error") %> prohibited this conversation_user from being saved:</h2>

      <ul>
        <% conversation_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
